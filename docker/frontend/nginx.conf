server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location = /health {
        access_log off;
        return 200 'ok';
    }

    location / {
        try_files $uri /index.html;
    }
}
